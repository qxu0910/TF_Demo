# 项目协作规则

## GitHub 同步

- 完成修改后使用 `auto-git-push` 技能；其可版本管理的源文件位于 `skills/auto-git-push/SKILL.md`。若个人技能尚未加载，直接读取该文件执行。

- 用户已授权：在本项目每次完成代码修改并执行适用的检查后，自动创建 Git 提交并推送到 `https://github.com/qxu0910/TF_Demo.git`，无需重复征求确认。
- 这是任务完成时的同步规则，不是文件保存监听器。
- 提交前检查 `git status` 和差异，只暂存本次任务相关文件；保留用户其他未提交更改。
- 不提交密码、令牌、私钥、真实环境变量文件、依赖目录或临时构建产物。
- 使用清晰的提交信息，并推送当前工作分支；首次推送设置对应上游分支。
- 不强制推送，不覆盖远端历史。若认证失败、远端冲突或分支保护阻止推送，保留本地提交并向用户说明具体阻碍。
- 完成后报告提交标识和推送结果；未经验证不得声称同步成功。
